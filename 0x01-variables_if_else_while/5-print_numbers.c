#include <stdio.h>
/**
 *  main - Prints all decimal numbers for 0 to 10
 *
 *  Return: Always (Success)
 */
int main(void)
{
	for (int i = 0; i < 10; i++)
		putchar(i);
	printf("\n");
	return (0);
}
