#include "main.h"
/**
 * mul - return the multplies of tw intiger
 * @a: the first intiger
 * @b: the second intiger
 * Return: multipication of the number
 */
int mul(int a, int b)
{
	return (a * b);
}
